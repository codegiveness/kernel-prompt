name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  validate:
    name: Validate (package.json + npm dry-run + skill files)
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - uses: actions/checkout@v4
        with:
          persist-credentials: false

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "22"

      - name: Install dependencies
        run: npm ci

      - name: Verify kernel skill files are present
        run: |
          for f in SKILL.md EXAMPLE.md REFORMULATIONS.md; do
            if [ ! -f "skills/engineering/kernel/$f" ]; then
              echo "❌ missing skills/engineering/kernel/$f"
              exit 1
            fi
            echo "✅ skills/engineering/kernel/$f present"
          done

      - name: Verify package.json name
        run: |
          NAME=$(node -p "require('./package.json').name")
          if [ "$NAME" != "@codegiveness/kernel-skill" ]; then
            echo "❌ package name is $NAME, expected @codegiveness/kernel-skill"
            exit 1
          fi
          echo "✅ package name is @codegiveness/kernel-skill"

      - name: Verify naming consistency
        run: |
          PKG_NAME=$(node -p "require('./package.json').name")
          if [ "$PKG_NAME" != "@codegiveness/kernel-skill" ]; then
            echo "❌ package.json name is $PKG_NAME, expected @codegiveness/kernel-skill"
            exit 1
          fi
          echo "✅ package.json name is @codegiveness/kernel-skill"

          PKG_BIN=$(node -p "JSON.stringify(require('./package.json').bin)")
          if [ "$PKG_BIN" != '{"kernel-skill":"bin/kernel-skill.mjs"}' ]; then
            echo "❌ package.json bin is $PKG_BIN, expected {\"kernel-skill\":\"bin/kernel-skill.mjs\"}"
            exit 1
          fi
          echo "✅ package.json bin is {\"kernel-skill\":\"bin/kernel-skill.mjs\"}"

          PLUGIN_NAME=$(node -p "require('./.claude-plugin/plugin.json').name")
          if [ "$PLUGIN_NAME" != "kernel-skill" ]; then
            echo "❌ plugin.json name is $PLUGIN_NAME, expected kernel-skill"
            exit 1
          fi
          echo "✅ plugin.json name is kernel-skill"

          PLUGIN_SKILLS=$(node -p "require('./.claude-plugin/plugin.json').skills.includes('./skills/engineering/kernel')")
          if [ "$PLUGIN_SKILLS" != "true" ]; then
            echo "❌ plugin.json skills does not include ./skills/engineering/kernel, got: $PLUGIN_SKILLS"
            exit 1
          fi
          echo "✅ plugin.json skills includes ./skills/engineering/kernel"

          MARKETPLACE_NAME=$(node -p "require('./.claude-plugin/marketplace.json').plugins[0].name")
          if [ "$MARKETPLACE_NAME" != "kernel-skill" ]; then
            echo "❌ marketplace.json first plugin name is $MARKETPLACE_NAME, expected kernel-skill"
            exit 1
          fi
          echo "✅ marketplace.json first plugin name is kernel-skill"

          SKILL_FM_NAME=$(grep -E "^name:" skills/engineering/kernel/SKILL.md | head -1 | sed 's/^name:[[:space:]]*//')
          if [ "$SKILL_FM_NAME" != "kernel" ]; then
            echo "❌ SKILL.md frontmatter name is '$SKILL_FM_NAME', expected kernel"
            exit 1
          fi
          echo "✅ SKILL.md frontmatter name is kernel"

          if ! grep -q 'const PKG = "@codegiveness/kernel-skill"' bin/kernel-skill.mjs; then
            echo "❌ bin/kernel-skill.mjs missing 'const PKG = \"@codegiveness/kernel-skill\"'"
            exit 1
          fi
          echo "✅ bin/kernel-skill.mjs has const PKG = @codegiveness/kernel-skill"

          if ! grep -q 'const SKILL_NAME = "kernel"' bin/kernel-skill.mjs; then
            echo "❌ bin/kernel-skill.mjs missing 'const SKILL_NAME = \"kernel\"'"
            exit 1
          fi
          echo "✅ bin/kernel-skill.mjs has const SKILL_NAME = kernel"

          if ! grep -q "codegiveness/kernel-skill" README.md; then
            echo "❌ README.md missing 'codegiveness/kernel-skill'"
            exit 1
          fi
          echo "✅ README.md contains codegiveness/kernel-skill"

          if ! grep -q "@codegiveness/kernel-skill" README.md; then
            echo "❌ README.md missing '@codegiveness/kernel-skill'"
            exit 1
          fi
          echo "✅ README.md contains @codegiveness/kernel-skill"

          CHANGESET_REPO=$(node -p "require('./.changeset/config.json').changelog[1].repo")
          if [ "$CHANGESET_REPO" != "codegiveness/kernel-skill" ]; then
            echo "❌ .changeset/config.json changelog repo is $CHANGESET_REPO, expected codegiveness/kernel-skill"
            exit 1
          fi
          echo "✅ .changeset/config.json changelog repo is codegiveness/kernel-skill"

          if ! grep -q 'NAME="kernel"' scripts/link-skills.sh; then
            echo "❌ scripts/link-skills.sh missing 'NAME=\"kernel\"'"
            exit 1
          fi
          echo "✅ scripts/link-skills.sh has NAME=kernel"

          if ! grep -q 'SRC="$REPO/skills/engineering/kernel"' scripts/link-skills.sh; then
            echo "❌ scripts/link-skills.sh missing 'SRC=\"$REPO/skills/engineering/kernel\"'"
            exit 1
          fi
          echo "✅ scripts/link-skills.sh has SRC=\$REPO/skills/engineering/kernel"

      - name: Verify plugin version matches package version
        run: |
          PV=$(node -p "require('./.claude-plugin/plugin.json').version")
          PJ=$(node -p "require('./package.json').version")
          if [ "$PV" != "$PJ" ]; then
            echo "❌ plugin.json version ($PV) != package.json version ($PJ)"
            exit 1
          fi
          echo "✅ versions match: $PV"

      - name: npm publish --dry-run
        run: npm publish --dry-run
